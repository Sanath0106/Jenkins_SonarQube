     pipeline {
        agent none
        stages {
         
          stage("build & sonarqube") {
            agent any
            steps {
              withSonarQubeEnv('My SonarQube Server') {
                bat 'mvn clean package sonar:sonar'
              }
            }
            
          }
        }
      }
